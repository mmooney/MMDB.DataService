@model object

@if (Model is Enum)
{
    @Html.EnumDropDownListFor(m => m)
}
else
{
    @Html.TextBox(
        "",
        ViewData.TemplateInfo.FormattedModelValue,
        new { @class = "text-box single-line" }
    )
}