@model MMDB.DataService.Data.Metadata.JobDataWorkflow

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>JobDataWorkflow</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.WorkflowName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.WorkflowName)
            @Html.ValidationMessageFor(model => model.WorkflowName)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
		<fieldset>
			<legend>Workflow Queue Steps</legend>
			<table>
				<tr>
					<th></th>
					<th></th>
				</tr>
				@foreach(var item in Model.WorkflowQueueList)
				{
					<
				}
			</table>
		</fieldset>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
